const fs=require('fs'),path=require('path'),acorn=require('acorn'),walk=require('acorn-walk');
const dir=path.resolve(__dirname,'../src/web'),checks=[];
for(const f of fs.readdirSync(dir).filter(n=>n.endsWith('.js')))acorn.parse(fs.readFileSync(dir+'/'+f,'utf8'),{ecmaVersion:'latest'});checks.push('All browser scripts parse, including imported production logic; invalid duplicate lexical declarations would fail parsing');
for(const [page,files] of [['index.html',['app.js','desktop-upgrade.js','trace-upgrade.js','toolkit-settings.js','design-system.js']],['production.html',['production.js','production-adapter.js']]]){const html=fs.readFileSync(dir+'/'+page,'utf8'),ids=[...html.matchAll(/\bid="([^"]+)"/g)].map(m=>m[1]);if(new Set(ids).size!==ids.length)throw Error('Duplicate DOM IDs in '+page);let missing=[];for(const file of files){const ast=acorn.parse(fs.readFileSync(dir+'/'+file,'utf8'),{ecmaVersion:'latest'});walk.simple(ast,{CallExpression(n){if(n.callee.type==='Identifier'&&n.callee.name==='$'&&n.arguments[0]?.type==='Literal'&&typeof n.arguments[0].value==='string'&&!ids.includes(n.arguments[0].value))missing.push(n.arguments[0].value);}});}if(missing.length)throw Error('Missing IDs: '+missing.join(','));for(const m of html.matchAll(/(?:src|href)="([^"#]+\.(?:js|css))"/g))if(!fs.existsSync(dir+'/'+m[1]))throw Error('Missing asset '+m[1]);}checks.push('Both workspaces have unique DOM IDs, valid literal control references and all referenced script/style assets');
const original=fs.readFileSync(dir+'/production-source.html','utf8'),current=fs.readFileSync(dir+'/production.html','utf8');const buttons=s=>[...s.matchAll(/<button\b[^>]*\bid="([^"]+)"/g)].map(m=>m[1]);const originalButtons=buttons(original),newButtons=new Set(buttons(current));if(originalButtons.some(id=>!newButtons.has(id)))throw Error('An original feature control is missing');const tabs=[...original.matchAll(/data-tab="([^"]+)"/g)].map(m=>m[1]);if(tabs.some(t=>!current.includes('data-tab="'+t+'"')))throw Error('An original section is missing');checks.push('All '+originalButtons.length+' originally ID-named HTML buttons and all 16 original sections are retained');
fs.writeFileSync(path.resolve(__dirname,'../tests/static-results.json'),JSON.stringify({passed:true,checks,originalButtons:originalButtons.length,tabs},null,2));for(const c of checks)console.log('PASS '+c);
